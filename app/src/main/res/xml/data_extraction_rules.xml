<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> tags to control what data is backed up.
        <include .../>
        <exclude .../>
        -->
    </cloud-backup>
    <!-- Device transfers are opt-in, and sharing data with other devices is disabled by default.
    To enable, add <device-transfer> below and uncomment the include. -->
    <!-- <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer> -->
</data-extraction-rules>